services:
  mongo:
    image: mongo:7
    container_name: ai-resume-analyzer-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: ai_resume_analyzer
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
